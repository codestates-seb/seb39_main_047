type Colors = {
  RED: string;
  BLACK: string;
  GREY: string;
};

const colors: Colors = {
  RED: '#FF0000',
  BLACK: '#000000',
  GREY: '#415867',
};

const theme = {
  colors,
};

export default theme;
